@model List<Model.User>
@{
    ViewData["Title"] = "Home Page";
}

<div class="container p-3">
    <div class="row pt-4">
        <div class="col-6">
            <h2 class="text-primary">
                Users
            </h2>
        </div>
        <div class="col-6 text-right">
            <a asp-controller="Home" asp-action="Create" class="btn btn-primary">New User</a>
        </div>
    </div>

    <br /><br />

    @if(Model.Count()>0)
    {
        <table class="table-bordered table-striped" style="width:100%">
            <thead>
                <tr>
                    <th>
                        ID
                    </th>
                    <th>
                        Login
                    </th>
                    <th>
                        Password
                    </th>
                    <th></th>
                </tr>
            </thead>
            <tbody>
                @foreach(var obj in Model)
                {
                    <tr>
                        <td>@obj.Id</td>
                        <td>@obj.Login</td>
                        <td>@obj.Password</td>
                        <td>
                            <a asp-route-id="@obj.Id" class="btn btn-primary" asp-action="Create">Редактировать</a>
                            <a asp-route-id="@obj.Id" class="btn btn-primary">Посмотреть</a>
                            <a asp-route-id="@obj.Id" class="btn btn-danger" asp-action="Delete">Удалить</a>
                        </td>
                    </tr>
                }
            </tbody>
        </table>
    }
    else
    {
        <p>Нет данных.</p>
    }
</div>
